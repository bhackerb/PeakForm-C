# Copy this file to .streamlit/secrets.toml (never commit that file)
# and fill in your keys.  On Cloud Run, set these as environment variables
# or use Secret Manager instead.

ANTHROPIC_API_KEY = "sk-ant-..."
